/// <reference types="node" />
import { FastifyPluginCallback } from "fastify";
import bcrypt from "bcrypt";
export declare type BcryptOptions = {
    saltOrRounds?: string | number;
};
declare type BcryptPlugin = {
    hash: {
        (data: string | Buffer): Promise<string>;
        (data: string | Buffer, callback: (err: Error | undefined, encrypted: string) => any): void;
    };
    compare: typeof bcrypt.compare;
};
declare const fastifyBcrypt: FastifyPluginCallback<Partial<BcryptOptions>, import("http").Server>;
export default fastifyBcrypt;
declare module "fastify" {
    interface FastifyRequest {
        bcrypt: BcryptPlugin;
    }
    interface FastifyInstance {
        bcrypt: BcryptPlugin;
    }
}
//# sourceMappingURL=index.d.ts.map